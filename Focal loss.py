import torch
import torch.nn.functional as F




# Example usage:
# y_true should be a tensor with ground truth labels, with the same shape as y_predicted
# y_predicted should be a tensor with raw output predictions (logits) from the model

